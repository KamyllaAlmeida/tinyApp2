<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <header>
    <% include  ./partials/_header %>
  </header>
  <body>
    <ul>
      <% for (var index in urls) { %>
          <li><a href="/urls/<%= index%>"><%= index%></a> - <a href="/urls/<%= index%>"><%= urls[index] %></a></li>
      <% }; %>

  </ul>
  </body>
</html>